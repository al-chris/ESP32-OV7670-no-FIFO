name=ESP32-OV7670-no-FIFO
version=0.1.0
author=al-chris
maintainer=al-chris
sentence=ESP32 OV7670 camera driver without FIFO, optional JPEG and MJPEG streaming support
paragraph=Driver for OV7670 using ESP32 I2S peripheral. Optional JPEG encoding (requires external encoder) and MJPEG streaming example included.
category=Signal Input/Output
url=
architectures=esp32
includes=OV7670.cpp,OV7670.h,I2SCamera.cpp,I2SCamera.h,BMP.h,JPEGEncoderWrapper.h,Config.h,XClk.cpp,I2C.h,BMP.h,DMABuffer.h
license=MIT

# Header files to be added automatically when the library is included via the Arduino IDE
#includes=OV7670.h,I2SCamera.h,BMP.h,JPEGEncoderWrapper.h,Config.h

# Optional compile-time macros (add to your project or build flags):
# -DOV7670_ENABLE_JPEG=0   Disable JPEG support even if encoder library present
# -DOV7670_MAX_JPEG_SIZE=65536   Increase maximum JPEG output buffer size
# -DOV7670_JPEG_QUALITY=75   Default JPEG quality

# Notes:
# If you want to use JPEG encoding, install the JPEGENC library (bitbank2/JPEGENC) or another encoder
# and ensure its header (e.g. JPEGENC.h) is available. The wrapper will auto-detect JPEGENC and use it.
